import numpy as np
class matrixspeed():
	def matrix(min_,max_,size):
    		matrix = np.random.RandomState(100).uniform(min_,max_,size)
    		matrix2 = np.random.RandomState(100).uniform(min_,max_,size)
    		return matrix*matrix2





